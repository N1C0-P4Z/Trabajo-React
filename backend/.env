# JWT Auth Backend Configuration

# JWT Configuration
JWT_SECRET=dev_jwt_secret_key_2024
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (for development)
FRONTEND_URL=http://localhost:3000

# PostgreSQL Database URL (used by Prisma)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/odontodb?schema=public
